import 'package:flutter/material.dart';

class AppColors {
  // Primary button colors (used for elevated buttons, FAB, etc.)
  static const Color primaryButtonBackground = Color(0xFF4B5563);
  static const Color primaryButtonForeground = Colors.white;
  
  // Secondary button colors (used for secondary actions)
  static const Color secondaryButtonBackground = Color(0xFF6B7280);
  static const Color secondaryButtonForeground = Colors.white;
  
  // Disabled button color
  static const Color disabledButtonBackground = Color(0xFF9CA3AF);
  
  // Border color for outlined buttons
  static const Color outlinedButtonBorder = Color(0xFFD1D5DB);
  
  // Accent color (blue)
  static const Color accent = Color(0xFF2563EB);
  
  // Status colors
  static const Color success = Color(0xFF4B5563);  // Same as primaryButtonBackground
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Colors.red;
}
